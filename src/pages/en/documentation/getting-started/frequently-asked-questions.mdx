# 经常被问的问题

import zh from '../../../../i18n/zh.ts';

<div className={'mt-5'}>
  {
    zh.faqs.map(item=>{
      return <div className={'flex flex-col mb-5'}>
        <span className={'text-lg mb-2'} style={{fontWeight:'bold'}}>{item.question}</span>
        <span>{item.answer}</span>
      </div>
    })
  }
</div>
